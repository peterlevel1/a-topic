var fs = require('fs');
var t = module.exports;

[ 'addQuote',
  'buildTree',
  'cookies',
  'ejs',
  'element',
  'extend',
  'parseString',
  'repeatString',
  'treeWalker',
  'utils',
  'watch'
].forEach(function (name) {
	t[name] = require('./' + name + '.js');
});